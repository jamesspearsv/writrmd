import { Inter } from 'next/font/google';
import { Roboto_Slab } from 'next/font/google';

// body font
export const inter = Inter({ weight: '400', subsets: ['latin'] });
// heading font
export const roboto_slab = Roboto_Slab({ weight: '700', subsets: ['latin'] });
